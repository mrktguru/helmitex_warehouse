# 🚀 Автоматический деплой Helmitex Warehouse

Этот документ описывает настройку автоматического деплоя на VPS через GitHub Actions.

## 📋 Оглавление

1. [Как это работает](#как-это-работает)
2. [Настройка на сервере](#настройка-на-сервере)
3. [Настройка GitHub Secrets](#настройка-github-secrets)
4. [Первый деплой](#первый-деплой)
5. [Использование](#использование)
6. [Откат изменений](#откат-изменений)
7. [Troubleshooting](#troubleshooting)

---

## Как это работает

**Процесс автоматического деплоя:**

1. Вы делаете `git push` в ветку `main`
2. GitHub Actions автоматически запускается
3. Подключается к вашему VPS по SSH
4. Создает бэкап базы данных
5. Обновляет код из репозитория
6. Пересобирает Docker образы
7. Перезапускает контейнеры
8. Проверяет здоровье приложения
9. В случае ошибки - откатывает изменения

**Время деплоя:** ~2-3 минуты

---

## Настройка на сервере

### Шаг 1: Подключитесь к серверу

```bash
ssh your_user@your_server_ip
```

### Шаг 2: Клонируйте репозиторий (если еще не сделали)

```bash
cd /root  # или любая другая директория
git clone https://github.com/mrktguru/helmitex_warehouse.git
cd helmitex_warehouse
```

### Шаг 3: Создайте .env файл

```bash
cp .env.example .env
nano .env
```

Заполните переменные окружения:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
OWNER_TELEGRAM_ID=your_telegram_id_here
DATABASE_URL=postgresql+asyncpg://warehouse:warehouse@db:5432/warehouse
LOG_LEVEL=INFO
DEBUG=False
```

### Шаг 4: Создайте SSH ключ для GitHub Actions

**На сервере:**

```bash
# Создаем новый SSH ключ для деплоя
ssh-keygen -t ed25519 -C "github-actions-deploy" -f ~/.ssh/github_actions_deploy -N ""

# Добавляем публичный ключ в authorized_keys
cat ~/.ssh/github_actions_deploy.pub >> ~/.ssh/authorized_keys

# Показываем приватный ключ (его нужно скопировать)
cat ~/.ssh/github_actions_deploy
```

**Скопируйте весь вывод** (включая `-----BEGIN OPENSSH PRIVATE KEY-----` и `-----END OPENSSH PRIVATE KEY-----`)

### Шаг 5: Сделайте deploy.sh исполняемым

```bash
chmod +x /root/helmitex_warehouse/deploy.sh
```

### Шаг 6: Первый запуск

```bash
cd /root/helmitex_warehouse
docker compose up -d --build
```

---

## Настройка GitHub Secrets

### Шаг 1: Откройте настройки репозитория

1. Перейдите на GitHub: `https://github.com/mrktguru/helmitex_warehouse`
2. Нажмите **Settings** (вверху справа)
3. В левом меню выберите **Secrets and variables** → **Actions**
4. Нажмите **New repository secret**

### Шаг 2: Добавьте секреты

Создайте следующие секреты:

| Имя секрета | Значение | Описание |
|-------------|----------|----------|
| `SSH_HOST` | `123.45.67.89` | IP адрес вашего VPS |
| `SSH_USERNAME` | `root` | Пользователь SSH (обычно `root`) |
| `SSH_PRIVATE_KEY` | `-----BEGIN OPENSSH...` | Приватный SSH ключ (из шага 4 выше) |
| `SSH_PORT` | `22` | SSH порт (обычно 22, можно не указывать) |
| `PROJECT_PATH` | `/root/helmitex_warehouse` | Путь к проекту на сервере |

**Важно:** При добавлении `SSH_PRIVATE_KEY`:
- Скопируйте **ВЕСЬ** ключ включая строки BEGIN и END
- Не добавляйте лишних пробелов или переносов строк

### Пример добавления секрета:

1. Нажмите **New repository secret**
2. Name: `SSH_HOST`
3. Secret: `123.45.67.89`
4. Нажмите **Add secret**
5. Повторите для всех остальных секретов

---

## Первый деплой

### Способ 1: Автоматический (через git push)

```bash
# На вашем локальном компьютере
git add .
git commit -m "Setup automatic deployment"
git push origin main
```

GitHub Actions автоматически запустится и выполнит деплой.

### Способ 2: Ручной запуск

1. Перейдите на GitHub в раздел **Actions**
2. Выберите workflow **Deploy to Production**
3. Нажмите **Run workflow** → **Run workflow**

---

## Использование

### Обычный деплой

Просто делайте push в main:

```bash
git add .
git commit -m "Your changes"
git push origin main
```

Деплой произойдет автоматически.

### Просмотр логов деплоя

1. Перейдите на GitHub в раздел **Actions**
2. Выберите последний запуск workflow
3. Откройте **Deploy to VPS** для просмотра логов

### Ручной деплой на сервере

Если нужно запустить деплой вручную:

```bash
ssh your_user@your_server_ip
cd /root/helmitex_warehouse
./deploy.sh
```

---

## Откат изменений

### Автоматический откат

Если деплой провалился, скрипт автоматически откатит изменения к предыдущей версии.

### Ручной откат

**На сервере:**

```bash
cd /root/helmitex_warehouse
./deploy.sh --rollback
```

**Или через git:**

```bash
cd /root/helmitex_warehouse
git reset --hard HEAD~1  # Откат на 1 коммит назад
docker compose down
docker compose up -d --build
```

### Восстановление базы данных из бэкапа

Бэкапы хранятся в `backups/`:

```bash
cd /root/helmitex_warehouse/backups
ls -lh  # Посмотреть список бэкапов

# Восстановить бэкап
gunzip -c db_backup_20241111_120000.sql.gz | \
docker compose exec -T db psql -U warehouse -d warehouse
```

---

## Troubleshooting

### Проблема: "Permission denied (publickey)"

**Решение:**

```bash
# На сервере проверьте права
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys

# Убедитесь, что ключ добавлен
cat ~/.ssh/authorized_keys
```

### Проблема: "Container health check failed"

**Решение:**

```bash
# Проверьте логи контейнеров
docker compose logs bot --tail=100
docker compose logs db --tail=50

# Проверьте статус
docker compose ps

# Перезапустите
docker compose restart
```

### Проблема: "Database connection failed"

**Решение:**

```bash
# Проверьте .env файл
cat /root/helmitex_warehouse/.env

# Проверьте, что база запущена
docker compose ps db

# Проверьте логи базы
docker compose logs db --tail=50
```

### Проблема: "Git pull failed"

**Решение:**

```bash
# На сервере
cd /root/helmitex_warehouse

# Сброс локальных изменений
git reset --hard origin/main
git clean -fd

# Обновление
git pull origin main
```

### Проблема: "Deployment takes too long"

Деплой занимает 2-3 минуты. Если дольше:

```bash
# Проверьте, не зависло ли что-то
docker compose ps

# Посмотрите системные ресурсы
htop
df -h
```

---

## Дополнительные команды

### Просмотр логов на сервере

```bash
# Логи бота (последние 100 строк)
docker compose logs bot --tail=100 -f

# Логи базы данных
docker compose logs db --tail=50 -f

# Все логи
docker compose logs -f
```

### Проверка статуса

```bash
# Статус контейнеров
docker compose ps

# Использование ресурсов
docker stats

# Проверка здоровья
docker compose exec bot python -c "print('Bot is running!')"
```

### Обновление зависимостей

```bash
# Обновить requirements.txt
pip freeze > requirements.txt
git add requirements.txt
git commit -m "Update dependencies"
git push origin main
```

### Очистка старых образов

```bash
# Удаление неиспользуемых образов
docker system prune -a

# Удаление старых бэкапов (>30 дней)
find /root/helmitex_warehouse/backups -name "*.sql.gz" -mtime +30 -delete
```

---

## Мониторинг

### Проверка работоспособности бота

```bash
# Отправьте боту команду /start в Telegram
# Или проверьте логи:
docker compose logs bot --tail=20
```

### Автоматические уведомления

GitHub Actions будет отправлять уведомления:
- ✅ При успешном деплое
- ❌ При ошибках деплоя

Уведомления придут на email, привязанный к GitHub.

---

## Безопасность

### Рекомендации:

1. **Используйте отдельного пользователя для деплоя**
   ```bash
   # Создайте пользователя deploy
   useradd -m -s /bin/bash deploy
   # Добавьте SSH ключ для него
   ```

2. **Ограничьте доступ SSH ключа**
   ```bash
   # Добавьте в authorized_keys с ограничениями:
   command="/root/helmitex_warehouse/deploy.sh",no-port-forwarding,no-X11-forwarding,no-agent-forwarding ssh-ed25519 AAAA...
   ```

3. **Используйте firewall**
   ```bash
   ufw allow 22/tcp
   ufw allow 5432/tcp  # Только если нужен внешний доступ к БД
   ufw enable
   ```

4. **Регулярно обновляйте систему**
   ```bash
   apt update && apt upgrade -y
   ```

---

## Контакты

Если возникли проблемы:
1. Проверьте логи в GitHub Actions
2. Проверьте логи на сервере: `docker compose logs`
3. Проверьте этот документ в разделе Troubleshooting

---

**Готово!** 🎉 Теперь каждый push в main будет автоматически деплоить изменения на ваш VPS.

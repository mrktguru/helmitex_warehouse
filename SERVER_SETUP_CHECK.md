# ✅ Чеклист проверки сервера перед первым деплоем

Выполните эти команды на сервере `38.244.194.181`, чтобы убедиться, что все готово для автоматического деплоя.

## 🔐 1. Проверка SSH доступа

```bash
# Проверьте, что SSH ключ добавлен в authorized_keys
cat ~/.ssh/authorized_keys | grep "claude-bot@helmitex"

# Должно показать публичный ключ
```

✅ Ожидаемый результат: Вы видите строку с ключом, содержащую `claude-bot@helmitex`

---

## 📁 2. Проверка директории проекта

```bash
# Проверьте, что директория существует
ls -la /root/helmitex_warehouse

# Перейдите в директорию
cd /root/helmitex_warehouse

# Проверьте git репозиторий
git status
git remote -v
```

✅ Ожидаемый результат:
- Директория существует
- Git репозиторий инициализирован
- Remote указывает на `github.com/mrktguru/helmitex_warehouse.git`

---

## 🔧 3. Проверка .env файла

```bash
# Проверьте наличие .env файла
ls -la /root/helmitex_warehouse/.env

# Посмотрите содержимое (без вывода секретов)
cat /root/helmitex_warehouse/.env | grep -E "^[A-Z_]+" | cut -d '=' -f 1
```

✅ Ожидаемый результат: Файл существует и содержит переменные:
- `TELEGRAM_BOT_TOKEN`
- `OWNER_TELEGRAM_ID`
- `DATABASE_URL`
- `LOG_LEVEL`
- `DEBUG`

**Если .env файла нет, создайте его:**

```bash
cd /root/helmitex_warehouse
cp .env.example .env
nano .env
```

Заполните переменные:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
OWNER_TELEGRAM_ID=ваш_telegram_id
DATABASE_URL=postgresql+asyncpg://warehouse:warehouse@db:5432/warehouse
LOG_LEVEL=INFO
DEBUG=False
```

---

## 🐋 4. Проверка Docker

```bash
# Проверьте версию Docker
docker --version

# Проверьте версию Docker Compose
docker compose version
# или
docker-compose --version

# Проверьте, что Docker запущен
docker ps
```

✅ Ожидаемый результат:
- Docker версии 20.10 или выше
- Docker Compose версии 2.0 или выше
- Команда `docker ps` выполняется без ошибок

**Если Docker не установлен:**

```bash
# Установите Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Запустите Docker
systemctl start docker
systemctl enable docker
```

---

## 🔑 5. Проверка прав доступа

```bash
# Сделайте deploy.sh исполняемым
chmod +x /root/helmitex_warehouse/deploy.sh

# Проверьте права
ls -la /root/helmitex_warehouse/deploy.sh
```

✅ Ожидаемый результат: `-rwxr-xr-x` (исполняемый файл)

---

## 📦 6. Создание директории для бэкапов

```bash
# Создайте директорию для бэкапов
mkdir -p /root/helmitex_warehouse/backups

# Проверьте
ls -la /root/helmitex_warehouse/ | grep backups
```

✅ Ожидаемый результат: Директория `backups` создана

---

## 🧪 7. Тестовый запуск (ОПЦИОНАЛЬНО)

Если хотите проверить, что все работает до автоматического деплоя:

```bash
cd /root/helmitex_warehouse

# Запустите контейнеры
docker compose up -d --build

# Подождите 15 секунд
sleep 15

# Проверьте статус
docker compose ps

# Посмотрите логи
docker compose logs bot --tail=50

# Остановите контейнеры (если хотите протестировать автоматический деплой)
docker compose down
```

✅ Ожидаемый результат:
- Контейнеры `bot` и `db` в статусе "Up"
- В логах нет критических ошибок
- Бот отвечает на команды в Telegram

---

## 🚀 8. Готовность к деплою

После выполнения всех проверок, сервер готов к автоматическому деплою!

### Что произойдет при следующем push в `main`:

1. GitHub Actions подключится к серверу по SSH ✅
2. Перейдет в `/root/helmitex_warehouse` ✅
3. Выполнит `git pull origin main` ✅
4. Пересоберет Docker образы ✅
5. Перезапустит контейнеры ✅
6. Проверит статус и логи ✅

---

## 📊 Быстрая проверка всех пунктов

Выполните этот скрипт для быстрой проверки:

```bash
#!/bin/bash

echo "🔍 Проверка готовности сервера к деплою..."
echo ""

# 1. SSH ключ
echo "1. SSH ключ:"
if cat ~/.ssh/authorized_keys | grep -q "claude-bot@helmitex"; then
    echo "   ✅ SSH ключ найден"
else
    echo "   ❌ SSH ключ НЕ найден"
fi

# 2. Директория проекта
echo "2. Директория проекта:"
if [ -d "/root/helmitex_warehouse" ]; then
    echo "   ✅ Директория существует"
else
    echo "   ❌ Директория НЕ существует"
fi

# 3. .env файл
echo "3. .env файл:"
if [ -f "/root/helmitex_warehouse/.env" ]; then
    echo "   ✅ .env файл существует"
else
    echo "   ❌ .env файл НЕ существует"
fi

# 4. Docker
echo "4. Docker:"
if command -v docker &> /dev/null; then
    echo "   ✅ Docker установлен: $(docker --version)"
else
    echo "   ❌ Docker НЕ установлен"
fi

# 5. Docker Compose
echo "5. Docker Compose:"
if docker compose version &> /dev/null; then
    echo "   ✅ Docker Compose установлен: $(docker compose version)"
elif docker-compose --version &> /dev/null; then
    echo "   ✅ Docker Compose установлен: $(docker-compose --version)"
else
    echo "   ❌ Docker Compose НЕ установлен"
fi

# 6. deploy.sh исполняемый
echo "6. deploy.sh:"
if [ -x "/root/helmitex_warehouse/deploy.sh" ]; then
    echo "   ✅ deploy.sh исполняемый"
else
    echo "   ❌ deploy.sh НЕ исполняемый"
    echo "   Выполните: chmod +x /root/helmitex_warehouse/deploy.sh"
fi

# 7. Директория backups
echo "7. Директория backups:"
if [ -d "/root/helmitex_warehouse/backups" ]; then
    echo "   ✅ Директория backups существует"
else
    echo "   ❌ Директория backups НЕ существует"
    echo "   Выполните: mkdir -p /root/helmitex_warehouse/backups"
fi

echo ""
echo "🎉 Проверка завершена!"
```

**Скопируйте этот скрипт на сервер и выполните:**

```bash
# Создайте файл проверки
cat > /tmp/check_server.sh << 'EOF'
[вставьте скрипт выше]
EOF

# Сделайте исполняемым и запустите
chmod +x /tmp/check_server.sh
/tmp/check_server.sh
```

---

## ❓ Если что-то не работает

1. Проверьте логи GitHub Actions: https://github.com/mrktguru/helmitex_warehouse/actions
2. Проверьте логи на сервере: `docker compose logs`
3. Проверьте права доступа: `ls -la ~/.ssh/authorized_keys`
4. Свяжитесь со мной, если нужна помощь!

---

## 🎯 Следующий шаг

После того, как все проверки пройдены, можно тестировать автоматический деплой!

Просто сделайте `git push origin main` или запустите workflow вручную через GitHub UI.
